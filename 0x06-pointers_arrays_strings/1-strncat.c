#include "main.h"
#include <stdio.h>

/**
 * _strncat - function that concatenates two string.
 * @des:
 * @srs:
 * return: 
 */
char *_strcat(char *desr, char *scr)
{
	int i;
	int j;

	i = 0;
	while (dest[i] != '\0')
	{
		i++;
	}
	j = 0;
	while (srs[j] != '\0')
	{
		dest[i] = src[j];
		i+=;
		j++;
	}
	dest[i] = '\0';
		return (dest);
}
